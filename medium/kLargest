#include<bits/stdc++.h>
vector<int> Klargest(vector<int> &a, int k, int n) {
    // Write your code here
    sort(a.begin(),a.end());
    vector<int> v;
    int j=0;
    for(int i=a.size()-1; j<k; j++, i--) {
        v.push_back(a[i]);
    }
    sort(v.begin(), v.end());
    return v;
}
